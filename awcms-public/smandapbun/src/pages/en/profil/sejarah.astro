---
import Layout from "../../../layouts/Layout.astro";
import { Icon } from "astro-icon/components";
import {
  getLocale,
  t,
  getLocalizedValue,
  getLocalizedPath,
  type Locale,
} from "../../../utils/i18n";
import { getProfilePageData } from "../../../lib/api";
import { sanitizeHTML } from "~/utils/sanitize";

const locale = getLocale(Astro.url) as Locale;
const profileData = await getProfilePageData();
const { history } = profileData;
---

<Layout title={getLocalizedValue(history.title, locale)}>
  <!-- Hero -->
  <section
    class="hero-gradient text-white py-16 md:py-20"
    style="--hero-image: url('/images/backgrounds/hero-campus.webp');"
    data-testid="history-hero"
  >
    <div class="container-custom">
      <nav class="text-sm text-blue-200 mb-4">
        <a href={getLocalizedPath("/", locale)} class="hover:text-white"
          >Beranda</a
        >
        <span class="mx-2">/</span>
        <a href={getLocalizedPath("/profil", locale)} class="hover:text-white"
          >{t("profile.title", locale)}</a
        >
        <span class="mx-2">/</span>
        <span class="text-white">{t("profile.history", locale)}</span>
      </nav>
      <h1 class="text-3xl md:text-4xl font-bold font-heading">
        {getLocalizedValue(history.title, locale)}
      </h1>
    </div>
  </section>

  <!-- Content -->
  <section
    class="section-photo py-16 bg-gray-50 dark:bg-slate-900 transition-colors duration-200"
    style="--section-image: url('/images/backgrounds/section-field.webp');"
    data-testid="history-content"
  >
    <div class="container-custom">
      <div class="max-w-4xl mx-auto">
        <!-- Main Content -->
        <div
          class="bg-white dark:bg-slate-800 rounded-2xl shadow-lg p-8 md:p-12 mb-12 transition-colors"
        >
          <div
            class="prose dark:prose-invert prose-lg max-w-none"
            set:html={sanitizeHTML(getLocalizedValue(history.content, locale))}
          />
        </div>

        <!-- Timeline -->
        <div class="mb-12">
          <h2
            class="text-2xl font-bold text-gray-900 dark:text-white mb-8 text-center"
          >
            {locale === "id" ? "Tonggak Sejarah" : "Historical Milestones"}
          </h2>
          <div class="relative">
            <!-- Timeline Line -->
            <div
              class="absolute left-4 md:left-1/2 top-0 bottom-0 w-1 bg-gradient-to-b from-primary-500 via-primary-400 to-primary-300 dark:from-primary-600 dark:via-primary-500 dark:to-primary-400 rounded-full"
            >
            </div>

            {
              history.milestones.map((milestone: any, index: number) => (
                <div
                  class={`relative flex items-center mb-8 ${index % 2 === 0 ? "md:flex-row-reverse" : ""}`}
                  data-testid={`milestone-${milestone.year}`}
                >
                  <div
                    class={`w-full md:w-1/2 ${index % 2 === 0 ? "md:pl-12" : "md:pr-12 md:text-right"} pl-16 md:pl-0`}
                  >
                    <div class="bg-white dark:bg-slate-800 p-6 rounded-xl shadow-md hover:shadow-lg transition-all duration-200">
                      <div class="text-2xl font-bold text-primary-600 dark:text-primary-400 mb-2">
                        {milestone.year}
                      </div>
                      <p class="text-gray-600 dark:text-gray-400">
                        {getLocalizedValue(milestone.event, locale)}
                      </p>
                    </div>
                  </div>
                  <div class="absolute left-4 md:left-1/2 w-8 h-8 bg-primary-600 dark:bg-primary-500 rounded-full transform md:-translate-x-1/2 -translate-x-1/2 border-4 border-white dark:border-slate-900 shadow-lg flex items-center justify-center transition-colors">
                    <Icon name="tabler:star" class="w-4 h-4 text-white" />
                  </div>
                </div>
              ))
            }
          </div>
        </div>

        <!-- Stats -->
        <div
          class="bg-primary-600 dark:bg-primary-800 rounded-2xl p-8 text-white text-center transition-colors"
        >
          <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
            <div>
              <div class="text-4xl font-bold">
                {new Date().getFullYear() - 1985}
              </div>
              <div class="text-blue-200 text-sm">
                {locale === "id" ? "Tahun Berdiri" : "Years Established"}
              </div>
            </div>
            <div>
              <div class="text-4xl font-bold">8500+</div>
              <div class="text-blue-200 text-sm">
                {locale === "id" ? "Total Alumni" : "Total Alumni"}
              </div>
            </div>
            <div>
              <div class="text-4xl font-bold">A</div>
              <div class="text-blue-200 text-sm">
                {locale === "id" ? "Akreditasi" : "Accreditation"}
              </div>
            </div>
            <div>
              <div class="text-4xl font-bold">100+</div>
              <div class="text-blue-200 text-sm">
                {locale === "id" ? "Prestasi" : "Achievements"}
              </div>
            </div>
          </div>
        </div>

        <!-- Back Button -->
        <div class="mt-8 text-center">
          <a
            href={getLocalizedPath("/profil", locale)}
            class="btn-outline dark:text-white dark:border-gray-600 dark:hover:bg-slate-800"
            data-testid="back-to-profile"
          >
            <Icon name="tabler:arrow-left" class="w-5 h-5 mr-2" />
            {t("common.back", locale)}
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>
